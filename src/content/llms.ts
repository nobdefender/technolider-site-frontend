// Содержимое /llms.txt и /llms-full.txt (формат llmstxt.org) — краткое и полное описание сайта для ИИ-ассистентов.
import { seo, services } from './seo';
import { routes, site, siteUrl } from './site';

const abs = (path: string) => new URL(path, siteUrl).toString();

export function llmsTxt(): string {
  return `# ${site.legalShort}

> ${seo.home.description}

Научно-производственное предприятие в Туле (Россия). Полный цикл: от идеи и чертежа до готовой серийной партии металлоконструкций и электроники. Собственный парк металлообрабатывающих станков, монтажные участки, испытательное и контрольное оборудование, отдел технического контроля (ОТК). Работаем с оборонными, телекоммуникационными и промышленными предприятиями. Партия — от 1 штуки. Ответ на заявку и оценка сроков и стоимости по чертежу — в течение 24 часов.

Контакты: телефон ${site.phone}, e-mail ${site.email}, адрес ${site.address}. ${site.hours}.
Реквизиты: ИНН ${site.inn}, КПП ${site.kpp}, ОГРН ${site.ogrn}.

## Услуги

${services.map((s) => `- [${s.name}](${abs(routes[s.key])}): ${seo[s.key].description}`).join('\n')}

## Компания

- [О компании](${abs(routes.about)}): ${seo.about.description}
- [Контакты и заявка](${abs(routes.contacts)}): ${seo.contacts.description}
- [Все услуги](${abs(routes.services)}): ${seo.services.description}

## Optional

- [Полное описание сайта для LLM](${abs('/llms-full.txt')})
- [Политика конфиденциальности](${abs(routes.privacy)}): ${seo.privacy.description}
- [Карта сайта](${abs('/sitemap.xml')})
`;
}

export function llmsFullTxt(): string {
  return `# ${site.legalShort} — полное описание сайта

> ${seo.home.description}

Сайт: ${siteUrl}
Язык: русский. Регион: Тула, Тульская область, Россия. Обслуживаем заказчиков по всей России.

## О предприятии

${site.legalFull} (${site.legalShort}) — научно-производственное предприятие полного цикла в сфере радиоэлектроники и механо-сборочных работ.

Наша специализация — создание сложных технических систем «под ключ»: от разработки конструкторской документации до серийного выпуска антенных систем и электронных модулей.

Предприятие оснащено собственным парком металлообрабатывающих станков, монтажными участками, испытательным и контрольным оборудованием, что позволяет жёстко контролировать качество на всех этапах — от заготовки до финишного тестирования готового изделия.

Работаем с оборонными, телекоммуникационными и промышленными предприятиями, решая задачи, где важна не просто деталь, а гарантированный результат в заданные сроки.

### Предприятие в цифрах

- 24 часа — ответ на заявку: оценка сроков и стоимости по чертежу.
- От 1 штуки — партия: опытные образцы и мелкие серии.
- ОТК — собственный отдел технического контроля: каждый узел проходит контроль.
- 100 % — полный цикл: от документации до серийного выпуска.

### Ключевые компетенции

1. Проектирование антенно-фидерных устройств любого диапазона.
2. Металлообработка с высокой точностью.
3. Контрактная сборка электроники по документации заказчика.
4. Разработка полного пакета конструкторской и технологической документации согласно действующим национальным стандартам Российской Федерации.

### Наша система

1. Инжиниринг — не просто берём ваш чертёж: можем улучшить конструкцию, сделав её технологичнее и дешевле в производстве.
2. Производство — собственный цех позволяет оперативно вносить правки и не зависеть от поставщиков-посредников.
3. Ответственность — несём полную ответственность за финальную сборку изделия, будь то антенная решётка или шкаф управления; каждый узел проходит ОТК.

## Как мы работаем

1. Заявка — вы присылаете ТЗ, эскиз или образец.
2. Анализ — определяем возможность разработки и/или изготовления и стоимость.
3. Документация — согласовываем чертежи (при необходимости разрабатываем свои).
4. Производство — запуск в производство с многоступенчатым контролем ОТК.
5. Результат — готовое изделие, упакованное и отправленное заказчику.

## Услуги

${services
  .map(
    (s) => `### ${s.name}

URL: ${abs(routes[s.key])}

${seo[s.key].description}

${s.items.map((i) => `- ${i}`).join('\n')}`,
  )
  .join('\n\n')}

## Контакты

- Телефон: ${site.phone}
- E-mail: ${site.email}
- Адрес: ${site.address}
- Режим работы: ${site.hours}
- Страница контактов и форма заявки: ${abs(routes.contacts)}

## Реквизиты

- Полное наименование: ${site.legalFull}
- ИНН: ${site.inn}
- КПП: ${site.kpp}
- ОГРН: ${site.ogrn}
- Юридический адрес: ${site.address}

## Страницы сайта

${(Object.keys(seo) as (keyof typeof seo)[])
  .filter((k) => !seo[k].noindex)
  .map((k) => `- [${seo[k].crumb}](${abs(seo[k].path)}) — ${seo[k].title}`)
  .join('\n')}
`;
}
